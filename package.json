{
  "name": "crosspoint-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "tunnel": "CLOUDFLARED_PATH=\"${CLOUDFLARED_PATH:-$(command -v cloudflared)}\" wrangler tunnel quick-start http://127.0.0.1:8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.14.0"
  }
}
