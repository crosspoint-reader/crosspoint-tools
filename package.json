{
  "name": "crosspoint-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "tunnel": "node scripts/tunnel.js",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "themes:manifest": "python3 scripts/generate-theme-manifest.py",
    "themes:icons": "python3 scripts/generate-theme-icons.py --from-freeink-sdk"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.14.0"
  }
}
